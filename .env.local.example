# Copy this file to .env.local and fill in your own values.
# CyberGuard runs fine without these — it falls back to local demo mode
# (accounts and scans are kept only in your browser).

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
